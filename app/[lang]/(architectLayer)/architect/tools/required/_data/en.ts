import type { WorkspacePageWords } from '@/lib/collection/types'

// Base language. A language we do not have falls back to this one.
export const en: WorkspacePageWords = {
  title: 'Required',
  lead: 'Without these the node does not run. Each one says what it is for and how to install it.',
}
