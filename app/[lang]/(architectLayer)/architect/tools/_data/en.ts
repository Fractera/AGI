import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Tools of this server',
  lead: 'What is installed on this machine, what the node needs and what is worth adding.',
}
