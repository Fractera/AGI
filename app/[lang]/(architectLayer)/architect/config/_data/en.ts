import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Config',
  lead: 'Project settings in one place: every service of the node takes its settings from here.',
}
