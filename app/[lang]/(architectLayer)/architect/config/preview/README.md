# `/architect/config/preview`

A live preview of the element (kind `elementPreview`), the owner's standard of 2026-09-24.
The address comes from `/api/node/preview-url?id=config` — `https://config.<zone>`.
