import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Application tools',
  lead: 'Ready-made working parts you can drop into a page — each one already built.',
}
