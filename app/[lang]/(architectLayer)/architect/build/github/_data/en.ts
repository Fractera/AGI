import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'GitHub',
  lead: 'The repository this node lives in: where the code goes and where it comes back from.',
}
