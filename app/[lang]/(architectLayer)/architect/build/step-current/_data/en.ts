import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Current step',
  lead: 'Where the work stands right now, so a new session continues instead of restarting.',
}
