import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Development steps: planned',
  lead: 'Work that is planned but not started: the queue the agent takes from.',
}
