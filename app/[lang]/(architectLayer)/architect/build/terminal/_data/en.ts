import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Terminal',
  lead: 'A live command line of this node — the place where a subscription is activated and a session is watched.',
}
