import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Skills',
  lead: 'Skills: procedures the agent follows instead of inventing its own each time.',
}
