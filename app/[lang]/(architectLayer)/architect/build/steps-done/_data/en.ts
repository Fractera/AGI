import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Development steps: finished',
  lead: 'Finished work with its proofs — what was built, how, and what it cost.',
}
