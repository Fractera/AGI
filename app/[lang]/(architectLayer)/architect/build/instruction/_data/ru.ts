import type { WorkspacePageWords } from '@/lib/collection/types'

export const ru: WorkspacePageWords = {
  title: 'Главная инструкция',
  lead: 'Главная инструкция, которую агент читает в начале каждой сессии.',
}
