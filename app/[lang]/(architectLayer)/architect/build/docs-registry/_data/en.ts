import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Document registry',
  lead: 'Every document this node keeps about its own construction, and when to open it.',
}
