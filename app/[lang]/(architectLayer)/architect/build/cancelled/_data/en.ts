import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Cancelled decisions',
  lead: 'Laws that no longer apply: what was cancelled, when, and what replaced it.',
}
