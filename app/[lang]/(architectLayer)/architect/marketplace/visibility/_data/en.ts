import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Visibility in the Marketplace',
  lead: 'Whether the network sees this node at all — and who exactly is allowed to.',
}
