import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Debugging',
  lead: 'Server-side state on demand: environment, versions, what the process actually sees.',
}
