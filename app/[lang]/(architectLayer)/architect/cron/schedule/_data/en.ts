import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Schedule',
  lead: 'Every job this node runs on its own: when it fires, what it does, and when it last did.',
}
