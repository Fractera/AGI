import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Blocks',
  lead: 'The catalogue of blocks a page is assembled from — each one shown as it really renders.',
}
