import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Design',
  lead: 'The look of the whole project is its own element, Design: colours, fonts, type, shapes and blocks. A save there re-styles every service of the node on the fly.',
}
