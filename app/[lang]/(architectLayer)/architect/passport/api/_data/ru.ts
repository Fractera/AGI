import type { WorkspacePageWords } from '@/lib/collection/types'

export const ru: WorkspacePageWords = {
  title: 'API',
  lead: 'Двери, которые узел открывает другим: адреса, что они принимают и что возвращают.',
}
