import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Requirements',
  lead: 'What the node needs from the machine it runs on: versions, ports, disk and network.',
}
