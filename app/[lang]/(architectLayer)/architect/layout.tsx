import type { ReactNode } from "react"

// ВЛОЖЕННЫЙ МАКЕТ СЕГМЕНТА `/architect` — второй этаж слоя.
//
// 🔒 ЗАЧЕМ ОН НУЖЕН ПУСТЫМ. Слой архитектора — два разных уровня, и у каждого
// своя работа. Внешний макет `(architectLayer)/layout.tsx` — это ЗАМОК: он
// решает, кого пускать, и ничего не рисует. Этот — ОБОЛОЧКА того, что внутри:
// сюда придут общая рамка страниц настроек, левое меню разделов и крошки.
//
// Разделение сделано до появления первой страницы намеренно. Заведи мы оба
// дела в одном макете — первая же страница потребовала бы рамку, рамка
// потребовала бы данные, данные потребовали бы чтения сессии, и замок,
// оставшийся статическим, уехал бы в динамику вместе со всем слоем.
//
// 🔒 ЯЗЫК ПРИХОДИТ СЕГМЕНТОМ `[lang]`, А НЕ НАСТРОЙКОЙ ЭТОГО МАКЕТА. Слой живёт
// внутри языкового маршрута с первой минуты: адрес страницы настроек будет
// `/{lang}/architect/...`, и мультиязычность у него не «добавится потом», а уже
// есть. Словарь слоя лежит рядом — `../_i18n/architect-layer.i18n.ts`.
export default function ArchitectLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
