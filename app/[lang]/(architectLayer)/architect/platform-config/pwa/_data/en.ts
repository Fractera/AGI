import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Installable app',
  lead: 'Whether the site offers to install itself as an app, and how it looks.',
}
