import type { WorkspacePageWords } from '@/lib/collection/types'

export const en: WorkspacePageWords = {
  title: 'Project start',
  lead: 'The path from an empty repository to a project that works — step by step, one action per step.',
}
