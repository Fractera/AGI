import type { WorkspacePageMeta } from '@/lib/collection/types'

export const meta: WorkspacePageMeta = {
  // 🎯 Первым делом человек запускает проект — раздел стоит выше остальных групп слоя.
  slug: 'project-start',
  order: 8,
}
