# `/architect/project-start/default-template/step-11` — a page of the project start path

The visible part of this page is **ported from the admin panel one to one** (step 274) and lives in
`_launch/pages/`. This folder holds only the wrapper our layer requires: `page.tsx`, `_data` and
`_components`.

🛑 **Do not edit the ported page here.** Its words live in `_launch/flow/`, its anatomy in
`_launch/ui/step-section.tsx`, its state in `_launch/server/launch-flow.ts`.
